// Existing code...

document.addEventListener('DOMContentLoaded', () => {
    gsap.to('.wave-section', {
        backgroundPosition: '100% 0',
        duration: 10,
        repeat: -1,
        ease: 'linear'
    });
});

// Existing code...
